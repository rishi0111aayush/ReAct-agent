# Boozo.ai — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework & server
fastapi>=0.115
uvicorn[standard]>=0.32

# HTTP client (sync, used for LLM API calls)
httpx>=0.27

# Environment variable loading
python-dotenv>=1.0

# Google OAuth + session middleware
authlib>=1.3
itsdangerous>=2.1

# HTML scraping (web_search tool)
requests>=2.32
beautifulsoup4>=4.12

# RAG — vector store + embeddings
chromadb>=1.5
sentence-transformers>=3.0

# Redis — caching, rate limiting, persistent user memory
redis>=5.0
